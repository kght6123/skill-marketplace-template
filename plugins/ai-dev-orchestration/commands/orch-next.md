`orch` の「今やること」を1件だけ表示してください。

引数: `$ARGUMENTS`（`15分` のように時間を指定できます）

```bash
node "$ORCH" sync
node "$ORCH" next --human            # 時間指定があれば --minutes N を付ける
```

表示するのは、人間の判断が必要なものと止まっている理由だけです。
順調に動いているAIの作業は出さないでください。
